package com.example.demo.Model;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.aspectj.weaver.ast.Var;

import java.util.Map;

public class VariantInfo {
    // resources indexed by version
   /* public Map<String, Liste> availableVariants;
    // public getter and setter

    public Map<String, Liste> getAvailableVariants() {
        return availableVariants;
    }

    public VariantInfo() {

    }
    public VariantInfo parseJson(String json) {
        try {
            ObjectMapper objectMapper = new ObjectMapper();
            return objectMapper.readValue(json, VariantInfo.class);
        } catch (Exception ex) {
            ex.printStackTrace();
        }
        return null;
    }*/
}